database:
  - name: database.json
    ipClassName: empty
    databaseName: compartiendomomentos

services:
  - type: web
    name: compartiendomomentos
    env: python
    region: oregon
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    healthCheckPath: /
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: FLASK_ENV
        value: production
      - key: SECRET_KEY
        value: compartiendomomentos2024secreto
      - key: MONGO_URI
        value: mongodb+srv://jorgeabusca_db_user:Lienan2005@cluster0.mxg4qcf.mongodb.net/?appName=Cluster0
